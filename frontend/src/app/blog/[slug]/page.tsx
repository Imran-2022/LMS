import Link from "next/link";
export default async function PostPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; return <main className="min-h-screen bg-[#f5f7f2] px-6 py-10 text-[#14221b] lg:px-10"><article className="mx-auto max-w-3xl"><Link href="/blog" className="text-sm font-bold">← Journal</Link><p className="mt-24 text-xs font-bold uppercase tracking-[.2em] text-[#e58c5a]">Field note {slug}</p><h1 className="mt-5 text-5xl font-semibold leading-tight tracking-[-.05em]">The value of a slower first draft</h1><div className="mt-10 space-y-6 text-lg leading-9 text-[#526259]"><p>Good work often begins before it looks like good work. The first pass is a place to notice what the idea wants to become.</p><p>Give it enough room to be unfinished. Clarity is not a personality trait; it is a practice we can return to.</p></div></article></main>; }
