import Link from "next/link";
import { strapiFetch, Course } from "@/lib/api";
import { enroll } from "@/app/actions";

export default async function CoursePage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; let course: Course = { id: Number(id), title: "Creative systems", description: "A practical course for turning scattered ideas into a clear, repeatable creative practice.", lessons: [{ id: 1, title: "Start with the question", order: 1 }, { id: 2, title: "Make a small commitment", order: 2 }, { id: 3, title: "Build the feedback loop", order: 3 }] }; try { const result = await strapiFetch<{ data: Course }>(`/api/courses/${id}?populate=lessons`); course = result.data; } catch {}
 return <main className="min-h-screen bg-[#f5f7f2] px-6 py-10 text-[#14221b] lg:px-10"><div className="mx-auto max-w-4xl"><Link href="/courses" className="text-sm font-bold">← All courses</Link><p className="mt-20 text-xs font-bold uppercase tracking-[.2em] text-[#e58c5a]">Course {id}</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.05em]">{course.title}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[#607066]">{course.description}</p><div className="mt-10 flex gap-3"><form action={enroll.bind(null, Number(id))}><button className="rounded-full bg-[#14221b] px-6 py-3 text-sm font-bold text-[#f5f7f2]">Enroll in course</button></form><span className="rounded-full bg-[#e5eadf] px-6 py-3 text-sm text-[#607066]">{course.lessons?.length || 3} lessons</span></div><section className="mt-20 border-t border-[#dce3d8]">{course.lessons?.map((lesson) => <Link href={`/my-courses/${id}/lessons/${lesson.id}`} key={lesson.id} className="flex items-center justify-between border-b border-[#dce3d8] py-6"><span className="flex gap-5"><span className="font-mono text-xs text-[#e58c5a]">0{lesson.order}</span><span className="font-semibold">{lesson.title}</span></span><span className="text-[#708074]">↗</span></Link>)}</section></div></main>;
}
