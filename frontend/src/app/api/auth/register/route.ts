import { NextResponse } from 'next/server';
export async function POST(request: Request) { const form = await request.formData(); const response = await fetch(`${process.env.STRAPI_URL || 'http://localhost:1337'}/api/auth/local/register`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: form.get('username'), email: form.get('email'), password: form.get('password') }) }); if (!response.ok) return NextResponse.redirect(new URL('/signup?error=1', request.url)); const data = await response.json(); const result = NextResponse.redirect(new URL('/my-courses', request.url)); result.cookies.set('strapi_jwt', data.jwt, { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', path: '/' }); result.cookies.set('user_role', data.user?.role?.type || 'student', { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', path: '/' }); return result; }
