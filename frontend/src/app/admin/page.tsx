import Link from "next/link";
import { strapiFetch } from "@/lib/api";
export default async function AdminPage() { let stats = { totalUsers: 0, totalCourses: 0, totalEnrollments: 0 }; try { const result = await strapiFetch<{ usersByRole: Record<string, number>; totalCourses: number; totalEnrollments: number }>("/api/admin/stats"); stats = { totalUsers: Object.values(result.usersByRole).reduce((sum, count) => sum + count, 0), totalCourses: result.totalCourses, totalEnrollments: result.totalEnrollments }; } catch {} return <main className="min-h-screen bg-[#f5f7f2] px-6 py-10 text-[#14221b] lg:px-10"><div className="mx-auto max-w-6xl"><Link href="/" className="text-sm font-bold">← LumaLearn</Link><h1 className="mt-20 text-5xl font-semibold tracking-[-.05em]">Admin dashboard</h1><div className="mt-14 grid gap-4 sm:grid-cols-3">{[['Users', stats.totalUsers], ['Courses', stats.totalCourses], ['Enrollments', stats.totalEnrollments]].map(([label, value]) => <div key={String(label)} className="border-t-2 border-[#14221b] py-5"><p className="text-sm text-[#607066]">{label}</p><p className="mt-3 text-3xl font-semibold">{value}</p></div>)}</div><Link href="/admin/users" className="mt-12 inline-block rounded-full bg-[#14221b] px-5 py-3 text-sm font-bold text-[#f5f7f2]">Manage users →</Link></div></main>; }
