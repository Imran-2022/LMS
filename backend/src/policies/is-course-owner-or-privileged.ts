export default async (policyContext: any) => { const user = policyContext.state.user; if (!user) return false; if (['admin', 'content_manager'].includes(user.role?.type)) return true; if (user.role?.type !== 'instructor') return false; const courseId = policyContext.params.id || policyContext.request.body?.data?.course; const course = await strapi.db.query('api::course.course').findOne({ where: { id: courseId }, populate: ['owner'] }); return Number(course?.owner?.id) === Number(user.id); };
